# Personal Memory Pipeline - PoC
# Python 3.10+

# NLP & Entity Extraction (optionnel - fallback regex si non installé)
# spacy>=3.7.0  # Décommenter si C++ Build Tools installés

# Embeddings: HTTP client to Weaviate transformers-inference sidecar (see docker-compose)
httpx>=0.27.0

# Graph Database
neo4j>=5.14.0

# Vector Store (v3 API - stable)
weaviate-client>=3.26.0,<4

# LLM (OpenAI)
openai>=1.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.28.0
rich>=13.0.0  # Pretty CLI output

# Agent orchestration (minimal agentic workflow)
langgraph>=0.2.0

# Local UI API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Google News RSS: resolve rss/articles links to publisher URLs for real thumbnails
googlenewsdecoder>=0.1.7
